```scheme
(define (next-odd n)
  (if (= (remainder n 2) 0)
      (+ n 1)
      (+ n 2)))

(define (next-primes n count)
  (cond ((= count 0))
        ((prime? n)
         (display n)
         (newline)
         (next-primes (next-odd n) (- count 1)))
        (else (next-primes (next-odd n) count))))

(define (prime? n)
  (= n (smallest-divisor n)))

(define (smallest-divisor n)
  (find-divisor n 2))

(define (find-divisor n test-divisor)
  (cond ((> (square test-divisor) n)
         n)
        ((divides? test-divisor n)
         test-divisor)
        (else
         (find-divisor n (+ test-divisor 1)))))

(define (divides? a b)
  (= (remainder b a) 0))

(define (search-for-primes n)
  (let ((start-time (real-time-clock)))
    (next-primes n 3)
    (- (real-time-clock) start-time)))

(define (n-count-prime n start)
  (next-primes n 3)
  (- (real-time-clock) start))
(define (prime? n)
  (= n (smallest-divisor n)))

(define (smallest-divisor n)
  (find-divisor n 2))

(define (find-divisor n test-divisor)
  (cond ((> (square test-divisor) n)
         n)
        ((divides? test-divisor n)
         test-divisor)
        (else
         (find-divisor n (+ test-divisor 1)))))

(define (next a)
  (if (= a 2) 3 (+ a 2)))

(define (divides? a b)
  (= (remainder b a) 0))
  
1 ]=> (search-for-primes 100000)
100003
100019
100043
;Value: 4

1 ]=> (search-for-primes 1000000)
1000003
1000033
1000037
;Value: 7

```
可以看到时间不仅没有缩短反而延长了，这是因为next的调用还需要时间，判断a是不是等于2也需要时间，这些所需的时间比直接将a+1的时间还要长

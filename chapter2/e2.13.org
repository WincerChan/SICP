区间1： x ± a
区间2： y ± b

乘的区间：
lower-bound = (x - a) * (y - b)
upper-bound = (x + a) * (y + b)
width = ((upper-bound) - (lower-bound)) / 2
      = b * x + a * y
center = ((upper-bound) + (lower-bound)) / 2
       = x * y + a * b

percent = width / center
